{
  "name": "kubejs",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@rspack/cli": "^1.7.0",
    "@rspack/core": "^1.7.0",
    "typescript": "^5"
  },
  "scripts": {
    "dev": "rspack build --watch",
    "build": "rspack build",
    "extract": "deno run @tooling/scripts/extract.ts",
    "debug:repl": "deno repl --inspect -r --allow-read --allow-write --allow-ffi --eval-file @tooling/scripts/repl.ts"
  },
  "dependencies": {
    "domhandler": "^5.0.3",
    "dprint-node": "^1.0.8",
    "type-fest": "^5.3.1"
  },
  "type": "module"
}